

on:
  workflow_dispatch: null
  pull_request:
    types: [opened, synchronize, labeled, unlabeled]

env:
  MATRIX: '{ "include": [{ "component": "rod"}, {"component": "jane" }] }'


jobs:
  example_run:
    runs-on: ubuntu-latest
    strategy:
      matrix: ${{ fromJson(env.MATRIX) }}
    steps:
      - run: |
          echo "${{ matrix.component }}"
